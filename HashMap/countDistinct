class Solution {
    static int countDistinct(int num[]) {
        // code here
        HashSet<Integer> set = new HashSet<>();

        for(int i=0 ; i<num.length; i++){
            set.add(num[i]);
        }
        
        return set.size();
    }
}

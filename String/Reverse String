class Solution {
    public static String reverseString(String s) {
        // code here
        String ans= new String("");
        int n=s.length();
        
        for(int i=n-1 ; i>=0 ; i--){
            ans=ans+s.charAt(i);
        }
        return ans;
    }
}

class Solution {
    public int titleToNumber(String s) {
      int n=s.length();
      int ans=0;
      for(int i=0 ; i<n ; i++){
        char curr = s.charAt(i);
        int currnum= curr-'A'+1;
        ans=ans*26+currnum;
      }
      return ans;
    }
}
